;;; про-шифрование.el --- Шифрование и безопасность в Emacs -*- lexical-binding: t; -*-
;;
;; Автор: Пётр <11111000000@email.com>
;; Версия: 1.0
;; Keywords: encryption, gpg, security, epa
;; URL: https://github.com/username/emacs.d/blob/main/инструменты/про-шифрование.el
;;
;;; Commentary:
;;
;; Этот файл настраивает шифрование в Emacs, следуя принципам
;; литературного программирования: код представлен как повествование,
;; где каждая секция объясняется и логически связана с остальными.
;;
;; Почему это важно? Безопасность данных — важный аспект работы с Emacs.
;; Здесь мы настраиваем GPG для прозрачного шифрования файлов через epa-file,
;; добавляем поддержку паролей и ключей. Это защищает чувствительные данные.
;;
;; Структура файла:
;;  0. Введение и зависимости
;;  1. Настройка GPG
;;  2. EPA для прозрачного шифрования
;;  3. Финал: Provide и ends here
;;
;; Использование: Загружается через (require 'про-шифрование) в init.el.
;; Требует установленного GPG.
;;
;;; Code:
;;;; Безопасность и шифрование

;; GNU Privacy Guard (GnuPG, GPG) — свободная программа для шифрования информации и создания электронных цифровых подписей. Разработана как альтернатива PGP и выпущена под свободной лицензией.
;; (setq epg-gpg-program "gpg2")

;;(custom-set-variables '(epg-gpg-program  "/run/current-system/profile/bin/gpg"))

;; или /home/az/.guix-profile/bin/gpg

;;;; Используется встроенный GPG-агент

(setenv "GPG_AGENT_INFO" nil)

;;;; Ввод пароля от ключа производится ч-з минибуфер

(setq-default epa-pinentry-mode 'loopback)
(setenv "GPG_AGENT_INFO" nil)

;;;; Список пар логин-пароль к ресурсам

;; это простой текстовый файл *.authinfo*, зашифрованый GPG2:

;; (setq auth-sources '("~/.authinfo.gpg"))

;;;; Прозрачное шифрование файлов

(require 'epa-file)

(provide 'про-шифрование)
;;; про-шифрование.el ends here
